from setuptools import setup

setup(
    name        = "ping",
    version     = "0.1.0",
    description = "ping and monitor tool",
    author      = "KIodine",
    license     = "MIT",
    packages    = ["ping",],
)
